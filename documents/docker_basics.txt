Docker is a platform for developing, shipping, and running applications in containers. Containers allow developers to package an application with all its dependencies into a standardized unit. Docker uses a client-server architecture. The Docker daemon manages containers on the host system. Docker images are built from Dockerfiles which contain instructions for creating the image.
